/* package com.intern.hub.ticket.core.domain.model.command;

public record UploadEvidenceCommand(
                Long ticketId,
                String tempKey,
                String fileType,
                Long fileSize) {
} */